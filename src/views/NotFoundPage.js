import React from 'react'
import { Typography } from '@material-ui/core'

const NotFoundPage = () => {
  return (
    <Typography variant='h2' align='center' style={{ marginTop: '40vh' }}>
      Page Not found :/
    </Typography>
  )
}

export default NotFoundPage
